{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "about",
  "description": "Capability for the standalone About window",
  "windows": ["about"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://incompl.com" }]
    }
  ]
}
